program if8;
var
  i, j, k : integer;
    bol:boolean;
    vector:array[2..10] of integer;

begin
  
    bol := false;
  For i:=0 to 10 do begin
        write('Voy por el ');
        writeln(x);
    end;
end.